let timer;
 let Debounce=(func)=>{
     return function (...args){
       const context=this;
       if(timer) clearTimeout(timer)
       timer=setTimeout(()=>{
        timer=null;
           func.apply(context,args)
       },500)
     } 
  }
let optimization =Debounce(this.filterForName)
